#include <iostream>
using namespace std;

int main() {
	int arr[5] = {1,3,2,5,4);
		int n=5;
		for (intj=0j<n-1;j++){
			if(arr[j]>arr[j+1]){
			   if(arr[j]>arr[j+1])
				   int temp = arr[j];
			       arr[j] = arr[j+1];
			       arr[j+1] + temp;
			}
		}
}
	for (int i = 0 ; i<n; i++){
		cout << arr[i] << "";
	}
return0;
